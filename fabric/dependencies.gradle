dependencies {
    implementation("fuzs.forgeconfigapiport:forgeconfigapiport-fabric:${config_version}")
    implementation("dev.isxander:yet-another-config-lib:${yacl_version}+${minecraft_version}-fabric")

    runtimeOnly("com.terraformersmc:modmenu:${fabric_modmenu_version}")

    include("fuzs.forgeconfigapiport:forgeconfigapiport-fabric:${config_version}")
    include("dev.isxander:yet-another-config-lib:${yacl_version}+${minecraft_version}-fabric")
    //include('com.github.FiguraMC.luaj:luaj-core:3.0.8-figura')
    //include('com.github.FiguraMC.luaj:luaj-jse:3.0.8-figura')
}